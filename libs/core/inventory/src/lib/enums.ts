export enum transferStatusDirection {
  pending = 'En attente',
  canceled = 'Annulé',
  closed = 'Clôturé',
  suspended = 'suspendu',
}

export enum orderItemStatus {
  pending = 'En attente',
  received = 'livré',
}


export enum itemTransferStatus {
  out = 'sortie',
  ready = 'prêt'
}
